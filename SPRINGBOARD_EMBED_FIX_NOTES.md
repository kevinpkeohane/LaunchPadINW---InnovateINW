# Springboard embed fix — 2026-07-15

Updated `springboard.html` and `styles.css`:

- Removed the rounded outer container around both Copper forms.
- Disabled iframe scrolling.
- Increased each embed's height so the submit button is visible while the page itself scrolls.
- Added desktop and mobile-specific form heights.
- Updated the Springboard stylesheet cache-buster to `styles.css?v=22`.

The Masterclass Google Form/dropdown project remains paused and is not included in this site package.
